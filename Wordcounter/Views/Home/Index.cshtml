<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Wordcount</title>
    <link rel='stylesheet' href='https://maxcdn.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap.min.css'>
    <script src="~/js/jquery-3.2.1.min.js"></script>
    <link rel='stylesheet' href="~/css/styles.css">
  </head>
<body>
    <div class = "container">
        <div class = "jumbotron">
            <h1>Wordcounter</h1>
            <h6>A simple site for finding how many times a word is in a sentence.</h6>
        </div>
        <div class = "row">
            <div class = "col-md-12">
                <form class="wordCountForm" action="/" method="post">
                    <div class='form-group'>
                      <label for='sentence'>Enter a Sentence: </label>
                      <input id='sentence' name='sentence' class='form-control' type='text'>
                      <label for='word'>Enter a Word: </label>
                      <input id='word' name='word' class='form-control' type='text'>
                    </div>
                    <button type="submit" class="btn">Find the sentences WordCount!</button>
                </form>
                <div id = "results">
                  <h2>Results: </h2>
                  @if (Model != null)
                  {
                  <h4 id = "sentenceDisplay">Input sentence: @Model.GetSentence()</h4><br>
                  <h4 id = "wordDisplay">Input word: @Model.GetWord()</h4><br>
                  <h4 id="countDisplay">Wordcount: @Model.GetWordCount()</h4><br>
                  }
                </div>
            </div>
        </div>
    </div>
    <div id="footer"><br> Alexander Neumann Epicodus February 16th, 2018</div>
</body>
